import { useEffect } from 'react';
import { useSelector, useDispatch } from 'react-redux';
import { getCartItems, clearCart } from '../features/cart/cartSlice';

const Order = () => {
  return <div>Payment Successful</div>;
};

export default Order;
